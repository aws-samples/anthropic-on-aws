{
  "name": "claude-bootstrap-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PKCE-mode bootstrap resource server for Claude Desktop: validates Entra access tokens and serves per-user configuration from S3.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "express": "^4.21.2",
    "jose": "^5.9.6"
  }
}
