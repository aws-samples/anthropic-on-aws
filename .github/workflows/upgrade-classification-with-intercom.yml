# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

name: upgrade-classification-with-intercom
on:
  schedule:
    - cron: 0 0 * * 1
  workflow_dispatch: {}
jobs:
  upgrade-classification-with-intercom:
    name: upgrade-classification-with-intercom
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20"
      - run: yarn install --check-files --frozen-lockfile
        working-directory: cdk-applications/classification-with-intercom
      - run: yarn upgrade
        working-directory: cdk-applications/classification-with-intercom
      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v4
        with:
          token: ${{ secrets.PROJEN_GITHUB_TOKEN }}
          commit-message: "chore: upgrade classification-with-intercom"
          branch: auto/projen-upgrade-classification-with-intercom
          title: "chore: upgrade classification-with-intercom"
          body: This PR upgrades classification-with-intercom
          labels: auto-merge, auto-approve
          author: github-actions <github-actions@github.com>
          committer: github-actions <github-actions@github.com>
          signoff: true
