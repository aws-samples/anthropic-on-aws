# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

name: upgrade-claude-tools-chatbot-client
on:
  schedule:
    - cron: 0 0 * * 1
  workflow_dispatch: {}
jobs:
  upgrade-claude-tools-chatbot-client:
    name: upgrade-claude-tools-chatbot-client
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20"
      - run: yarn install --check-files --frozen-lockfile
        working-directory: claude-tools-chatbot/claude-tools-chatbot-client
      - run: yarn upgrade
        working-directory: claude-tools-chatbot/claude-tools-chatbot-client
      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v4
        with:
          token: ${{ secrets.PROJEN_GITHUB_TOKEN }}
          commit-message: "chore: upgrade claude-tools-chatbot-client"
          branch: auto/projen-upgrade-claude-tools-chatbot-client
          title: "chore: upgrade claude-tools-chatbot-client"
          body: This PR upgrades claude-tools-chatbot-client
          labels: auto-merge, auto-approve
          author: github-actions <github-actions@github.com>
          committer: github-actions <github-actions@github.com>
          signoff: true
