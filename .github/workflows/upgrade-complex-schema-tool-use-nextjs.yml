# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

name: upgrade-complex-schema-tool-use-nextjs
on:
  schedule:
    - cron: 0 0 * * 1
  workflow_dispatch: {}
jobs:
  upgrade-complex-schema-tool-use-nextjs:
    name: upgrade-complex-schema-tool-use-nextjs
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20"
      - run: yarn install --check-files --frozen-lockfile
        working-directory: complex-schema-tool-use/nextjs-typescript-example
      - run: yarn upgrade
        working-directory: complex-schema-tool-use/nextjs-typescript-example
      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v4
        with:
          token: ${{ secrets.PROJEN_GITHUB_TOKEN }}
          commit-message: "chore: upgrade complex-schema-tool-use-nextjs"
          branch: auto/projen-upgrade-complex-schema-tool-use-nextjs
          title: "chore: upgrade complex-schema-tool-use-nextjs"
          body: This PR upgrades complex-schema-tool-use-nextjs
          labels: auto-merge, auto-approve
          author: github-actions <github-actions@github.com>
          committer: github-actions <github-actions@github.com>
          signoff: true
