# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

name: upgrade-metaprompt-generator-site
on:
  schedule:
    - cron: 0 0 * * 1
  workflow_dispatch: {}
jobs:
  upgrade-metaprompt-generator-site:
    name: upgrade-metaprompt-generator-site
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20"
      - run: yarn install --check-files --frozen-lockfile
        working-directory: metaprompt-generator/site
      - run: yarn upgrade
        working-directory: metaprompt-generator/site
      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v4
        with:
          token: ${{ secrets.PROJEN_GITHUB_TOKEN }}
          commit-message: "chore: upgrade metaprompt-generator-site"
          branch: auto/projen-upgrade-metaprompt-generator-site
          title: "chore: upgrade metaprompt-generator-site"
          body: This PR upgrades metaprompt-generator-site
          labels: auto-merge, auto-approve
          author: github-actions <github-actions@github.com>
          committer: github-actions <github-actions@github.com>
          signoff: true
