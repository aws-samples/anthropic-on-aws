# Local-only upstream-watch tooling (see docs/upstream-watch.md). Kept out of the
# repo so operators run it locally / in their own CI rather than shipping it here.
# (Anchored: does not affect the tracked cdk/scripts/.)
/scripts/
